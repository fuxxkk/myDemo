<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ch.mapper.UserMapper">
    
    <resultMap id="usermap" type="TestUser">
        <id column="id" property="id"/>
        <result column="name" property="username"/>
        <result column="password" property="password"/>
        <collection property="roles" ofType="UserRole" select="getRoles" column="id"/>
    </resultMap>

    <select id="getLoginUSer" parameterType="string" resultMap="usermap">
        select * from tb_user where name=#{username}
    </select>
    
    <select id="getRoles" parameterType="int" resultType="UserRole">
		SELECT * from tb_roles where id=#{id}
	</select>

</mapper>